# Plans

- Treat every file in this directory as non-normative until the referenced code,
  config/schema version, tests, and artifact status exist.
- Preserve historical decisions, but add a clear current-status note instead of
  rewriting history when a plan is completed or superseded.
- Link to the owning package and acceptance tests. Never direct an agent to
  implement every phase unless the user explicitly selects that scope.

